<!doctype html>
<html class="h-100">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <title>{% block title %}Fantecolo Tennis{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>
<body class="d-flex flex-column h-100 text-center">
    <nav class="navbar navbar-expand-md navbar-dark mb-4">
        <a class="navbar-brand" href="{{ path('home') }}">
            FANTECOLO tennis
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault"
                aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExampleDefault">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('prenotazione_index') }}">Tabellone <span
                                class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('regolamento') }}">Regolamento</a>
                </li>
            </ul>
            {% if is_granted('ROLE_USER') %}
                <a class="text-light mr-1"><i class="fas fa-user"></i> {{ app.user.username }} </a>
                <a class="btn btn-danger" href="{{ logout_path() }}"><i class="fas fa-sign-out-alt ml-2"></i></a>
            {% else %}
                <a class="btn btn-primary mr-2" href="{{ path('app_login') }}"><i class="fas fa-sign-in-alt"></i> Entra</a>
                <a class="btn btn-primary" href="{{ path('giocatore_registrazione') }}">Registrati</a>
            {% endif %}

        </div>
    </nav>
<div class="container">
    {# read and display all flash messages #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
</div>
{% block body %}

{% endblock %}
{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>

